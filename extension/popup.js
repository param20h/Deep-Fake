// Simple popup logic if we want to show last analysis or status
document.addEventListener('DOMContentLoaded', () => {
    // Currently the main action happens via context menu, so the popup is just informational.
    console.log("Popup loaded.");
});
